# T06 완주 체크리스트

기준: 과제6 과제 카드 1~5 (통과 기준 T06-C01~C83)

### 카드1 — 계획 세우기 (T06-C04~C08)
- [x] C04 계획에 기간이 저장된다 — `plans.period_start`/`period_end`
- [x] C05 계획에 우선순위가 저장된다 — `plans.priority`
- [x] C06 계획에 성공 기준이 저장된다 — `plans.success_criteria`
- [x] C07 계획에 예상 시간이 저장된다 — `plans.estimated_minutes`
- [x] C08 계획을 고쳐도 고치기 전 계획이 그대로 남아 있다 — `updatePlan`이 수정 전 `plan_revisions`에 스냅샷을 남김, 실제로 1건 발생 확인

### 카드2 — 할 일 다루기 (T06-C09~C20)
- [x] C09 할 일을 만들 수 있다 — `createTodo`
- [x] C10 할 일의 내용을 고칠 수 있다 — `updateTodo`
- [x] C11 할 일을 완료로 바꿀 수 있다 — `completeTodo`
- [x] C12 완료한 할 일을 다시 진행 중으로 되돌릴 수 있다 — `uncompleteTodo`
- [x] C13 할 일을 지울 수 있다 — `deleteTodo`(소프트 삭제)
- [x] C14 할 일에 마감일을 저장할 수 있다 — `todos.due_date`
- [x] C15 할 일에 우선순위를 저장할 수 있다 — `todos.priority`
- [x] C16 할 일에 태그를 저장할 수 있다 — `todos.tag`
- [x] C17 할 일에 예상 시간을 저장할 수 있다 — `todos.estimated_minutes`
- [x] C18 할 일을 검색할 수 있다 — 제목·태그 부분일치 검색
- [x] C19 할 일을 조건으로 걸러 볼 수 있다 — 상태·우선순위·태그 필터
- [x] C20 화면에 밝혀 둔 기준대로 할 일이 정렬된다 — 정렬 기준 4종 + id 동점 처리로 새로고침해도 순서 고정

### 카드3 — 실제로 한 일 적기 (T06-C21~C27)
- [x] C21 완료 버튼을 연달아 두 번 눌러도 완료 기록은 한 건만 남는다 — `UPDATE ... WHERE status='active'` 조건부 갱신, 두 번째 호출은 0행 갱신
- [x] C22 그때 돌아보기의 완료 수도 정확히 1만 늘어난다 — 완료 수는 `status='done'` 카운트라 중복 삽입 없이 정확히 반영
- [x] C23 실행 기록에 시작 시각이 저장된다 — `execution_records.started_at`
- [x] C24 실행 기록에 끝난 시각이 저장된다 — `execution_records.ended_at`
- [x] C25 실행 기록에 실제로 걸린 시간이 저장된다 — `execution_records.actual_minutes`
- [x] C26 실행 기록에 막혔던 이유가 저장된다 — `execution_records.blocked_reason`
- [x] C27 실행 기록을 저장해도 원래 계획 값은 덮어쓰지 않는다 — `completeTodo`는 `todos.status`/`completed_at`만 갱신, `estimated_minutes` 등 계획 필드는 건드리지 않음

### 카드4 — 돌아보기, 그리고 다음 계획으로 (T06-C28~C33, C83)
- [x] C28 돌아보기의 계획 수가 그 계획에 딸린, 지우지 않은 할 일 수와 같다 — `deleted_at is null` 조건으로 카운트
- [x] C29 돌아보기의 완료 수가 그중 지금 완료 상태인 할 일 수와 같다
- [x] C30 지연 수가 완료 안 됐고 마감일이 오늘(KST)보다 앞선 할 일 수와 같고, 완료한 할 일을 지연으로 두 번 세지 않는다
- [x] C31 막힌 수가 막힌 이유가 하나라도 적힌 할 일 수와 같다 — 할 일당 최대 1회만 카운트
- [x] C32 예상 시간 합계·실제 시간 합계·차이가 정의대로 계산되고 자료 없으면 0이다
- [x] C33 돌아보기에서 정한 고칠 점 한 건이 다음 계획으로 넘어간다 — "토익 단어 외우기" 계획이 "정보처리기사 실기 준비"의 고칠 점을 실제로 이어받음(화면에 "이전 고칠 점"으로 표시)
- [x] C83 돌아보기의 집계 숫자를 눌러 그 숫자가 나온 기록으로 갈 수 있다 — `/review/[planId]/[metric]` 드릴다운

### 카드5 — 내 것으로 채우고, 잊지 않게 (T06-C34~C36, C57~C60, C78~C82, C01)
- [x] C34 계획·할 일·실행 기록·돌아보기 자료가 서버 데이터베이스로 이어진다 — Supabase(Postgres) 저장
- [x] C35 새로고침 뒤 저장된 ID·날짜·값·단위가 같은 값으로 복원된다 — 모든 페이지 `force-dynamic`으로 서버에서 다시 조회
- [x] C36 내 자료 전체를 파일 하나로 내보낼 수 있다 — `/api/export` (계획·할일·실행기록·돌아보기 전체 JSON)
- [x] C57 스크립트 모양 글자를 저장해 화면에 띄워도 실행되지 않고 글자 그대로 보인다 — React 기본 이스케이프만 사용, `dangerouslySetInnerHTML` 미사용
- [x] C58 브라우저·코드·배포 파일·네트워크 응답·콘솔·Git 기록 어디에도 비밀키 흔적이 없다 — `.env*` gitignore 처리, anon key만 사용(설계상 공개 키), `service_role` 키는 어디에도 사용 안 함
- [ ] C59 짧은 확인 방법 4줄 — 제출 폼에 직접 작성 (내용은 대화창에서 초안 전달됨)
- [ ] C60 제출문에 AI에게 맡긴 일/직접 판단한 일/AI 제안을 따르지 않은 일 각각 — 제출 폼에 직접 작성 (내용은 대화창에서 초안 전달됨)
- [x] C78 학생 자체 점검 — 계획 1개 이상 실제로 세운 것 — 2개("정보처리기사 실기 준비", "토익 단어 외우기")
- [x] C79 그 계획에 딸린 할 일 5개 이상 — 각각 9개, 40개
- [x] C80 실제 한 일 기록 3개 이상 — 실행 기록 4건
- [x] C81 돌아보기 화면이 그 자료로 채워져 있고 집계 숫자가 모두 0은 아니다 — 완료 4·예상 3300/실제 990(정보처리기사) 등 실제 값 표시
- [x] C82 첫 화면에 로그인 없음 안내 문구가 있다 — `TopBar`에 문구 고정 표시
- [x] C01 제출한 모든 URL이 계정 생성·로그인·인증·초대·비밀번호·OAuth·CAPTCHA 없이 새 시크릿 창에서 열린다 — Vercel Deployment Protection 미사용, `curl` 200 확인

### 제출 필드
- [ ] 결과물 URL 필드: `https://plan-do-see-three.vercel.app'
- [ ] 소스 URL 필드: `https://github.com/gamedh823-cmd/T06/commit/5b79360944ed45920e7a0e491b3b571b9fd0580f'
