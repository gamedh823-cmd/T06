# T06 검증안내서

## 무엇을 만들었나

계획(Plan) → 실제로 한 일(Do) → 돌아보기(See)가 하나로 이어지는, 로그인 없는 개인 다이어리 앱입니다. 로그인은 붙이지 않았고(로그인·잠금은 T07에서 다룸), 모든 기능이 그대로 돌아가게 만들었습니다. 자료는 Supabase(Postgres) 서버 데이터베이스에 저장되고, 새로고침해도 그대로 남습니다.

- 계획: 기간·우선순위·성공 기준·예상 시간을 저장하고, 고쳐도 고치기 전 계획이 `plan_revisions`에 그대로 남습니다.
- 할 일: 만들기·고치기·완료·되돌리기·지우기가 가능하고, 마감일·우선순위·태그·예상 시간을 저장하며 검색·필터·정렬됩니다.
- 실행 기록: "완료 처리" 한 번에 시작~종료 시각과 막힌 이유를 함께 저장합니다. 완료 버튼을 연달아 두 번 눌러도(`WHERE status='active'` 조건부 UPDATE) 기록은 1건만 남습니다.
- 돌아보기: 계획별 계획 수·완료 수·지연 수·막힌 수·예상/실제 시간 차이를 집계하고, 각 숫자를 누르면 그 숫자를 만든 실제 기록으로 이동합니다(드릴다운). 고칠 점 한 줄을 남기면 다음 계획을 만들 때 그대로 가져올 수 있습니다.
- 내보내기: 전체 자료(계획·할 일·실행 기록·돌아보기)를 JSON 파일 하나로 내려받을 수 있습니다.

## 제출물

- 결과물 URL: `https://plan-do-see-three.vercel.app`
- 소스 저장소 URL: `https://github.com/gamedh823-cmd/T06/commit/5b79360944ed45920e7a0e491b3b571b9fd0580f`
- DB 스키마·집계 규칙 문서: [contracts/pds-schema-v2.json](contracts/pds-schema-v2.json)

## 재현·통과 확인 — 계획 세우기 (T06-C04~C08)

1. 어디로 가나요: 결과물 URL 접속 → "+ 새 계획"
2. 무엇을 하나요(3단계 이내): 제목·기간·우선순위·예상 시간·성공 기준 입력 → "계획 만들기" → 계획 상세에서 "계획 수정"으로 값 하나 바꾸고 저장
3. 무엇이 보이면 통과: 계획 카드에 기간·우선순위·예상 시간·성공 기준이 그대로 보이고, 상세 화면에 "수정 이력 N건 — 고치기 전 계획 보기"를 펼치면 고치기 전 값이 그대로 남아 있음
4. 안 될 때 무엇이 보이나요: 필수 항목(제목·기간·성공 기준)을 비우면 브라우저가 제출을 막고 빨간 테두리로 표시함

## 재현·통과 확인 — 할 일 다루기 (T06-C09~C20)

1. 어디로 가나요: 계획 상세 화면 → "+ 할 일 추가"
2. 무엇을 하나요: 제목·마감일·우선순위·태그·예상 시간 입력 후 저장 → 목록에서 "수정"으로 고치기 → "검색·필터·정렬"에서 조건 바꿔보기
3. 무엇이 보이면 통과: 만든 할 일이 목록에 뜨고, 완료 체크·되돌리기·삭제가 즉시 반영되며, 검색/상태/우선순위/태그/정렬 조건을 바꾸면 목록이 그 조건대로 다시 정렬됨(같은 조건이면 새로고침해도 순서가 안 바뀜)
4. 안 될 때 무엇이 보이나요: 조건에 맞는 할 일이 없으면 "조건에 맞는 할 일이 없습니다" 문구가 남음

## 재현·통과 확인 — 실제로 한 일 적기 (T06-C21~C27)

1. 어디로 가나요: 할 일 카드의 "완료 처리" 펼침
2. 무엇을 하나요(3단계 이내): 시작 시각·종료 시각(+막힌 이유) 입력 → "완료로 저장" → 같은 할 일에서 "완료 처리"를 다시 두 번 빠르게 눌러보기
3. 무엇이 보이면 통과: 할 일 아래에 실행 기록(시작~종료, 실제 걸린 시간, 막힌 이유)이 쌓이고, 두 번 눌러도 기록은 1건만 남으며 계획에 저장된 예상 시간 값은 그대로임
4. 안 될 때 무엇이 보이나요: 시작·종료 시각을 비우면 저장이 거부됨

## 재현·통과 확인 — 돌아보기와 드릴다운 (T06-C28~C33, C83)

1. 어디로 가나요: 상단 "돌아보기" 탭
2. 무엇을 하나요: 계획별 집계 카드에서 숫자(계획 수/완료 수/지연 수/막힌 수/예상·실제 시간) 중 하나 클릭 → 고칠 점 입력 후 "기록" → 새 계획을 만들 때 "이전 돌아보기에서 넘어온 고칠 점"에서 방금 남긴 줄 선택
3. 무엇이 보이면 통과: 숫자를 누르면 그 숫자를 만든 실제 할 일·실행 기록 목록으로 이동하고, 고칠 점이 다음 계획 상세 화면에 "이전 고칠 점"으로 표시됨
4. 안 될 때 무엇이 보이나요: 고칠 점을 비운 채 "기록"을 누르면 저장이 거부됨(필수 입력)

## 재현·통과 확인 — 내 자료로 채우기·내보내기 (T06-C34~C36, C78~C82)

1. 어디로 가나요: 결과물 URL 첫 화면, 상단 "내보내기"
2. 무엇을 하나요: 새로고침해서 계획·할 일·완료·돌아보기 숫자가 그대로인지 확인 → "내보내기" 클릭
3. 무엇이 보이면 통과: 첫 화면 상단에 "지금은 로그인이 없어 링크를 아는 사람은 누구나 볼 수 있습니다..." 안내가 있고, 실제 계획 2개(각각 할 일 9개·40개, 5개 이상)·완료 기록 4건(3건 이상)이 들어있으며, "내보내기"를 누르면 전체 자료가 담긴 JSON 파일 하나가 내려받아짐
4. 안 될 때 무엇이 보이나요: 계획이 하나도 없으면 "아직 계획이 없습니다..." 안내만 보임

## AI와 내 판단

1. AI에게 맡긴 일: Next.js + Supabase 스키마·서버 액션 설계, 정렬 동점 처리·검색 필터 인젝션 버그 수정, 할 일 수정 후 리다이렉트 안 되던 버그 수정, 돌아보기 입력 필수값 누락 버그 수정, Supabase 프로젝트·Vercel 배포 자동화, 계획 카드 시간 배분 계산
2. 내가 직접 판단한 일: (직접 채워주세요 — 예: 어떤 실제 계획/할 일로 채울지, 완료 기록 시간이 현실적인지 검토해서 고친 것, 색상·디자인 방향 결정, 할 일을 하루 단위로 쪼갤지 5일 묶음으로 할지 판단한 것 등)
3. AI 말을 안 들은 일 (없으면 이유): (직접 채워주세요)
